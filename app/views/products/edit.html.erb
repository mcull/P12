<%= render partial: "shared/header", locals: {obj: @product, name: "Product: " + @product.name} %>
<%= render partial: "form", locals: {product: @product} %>
<%= render partial: "product_photos/photo_grid", locals: {product: @product} %>
